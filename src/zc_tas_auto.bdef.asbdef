projection implementation in class ZBP_C_TAS_AUTO unique;
strict ( 2 );
use draft;
define behavior for ZC_TAS_AUTO alias ZcTasAuto
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}